"use client";

type Props = object;

export default function NavBar({}: Props) {
  return (
    <nav className="h-screen">
      <div className="border-b border-black h-1/12"></div>
    </nav>
  );
}
